% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Ne_sensitivity.R
\name{Ne_sensitivity_Vc}
\alias{Ne_sensitivity_Vc}
\title{Sensitivity of Ne/N to clonal reproductive variance in one stage}
\usage{
Ne_sensitivity_Vc(
  T_mat,
  F_vec,
  D,
  d,
  stage_index = NULL,
  stage_name = NULL,
  Vc_range = seq(0.5, 5, by = 0.5),
  Vc_fixed = 1,
  Vk_over_k = NULL,
  a = 0,
  L = NULL,
  Ne_target = 5000,
  population = NULL
)
}
\arguments{
\item{T_mat}{Survival/transition matrix (s x s).}

\item{F_vec}{Fecundity vector (length s).}

\item{D}{Stage frequency distribution (length s, sums to 1).}

\item{d}{Per-stage clonal fraction vector (length s). Required.}

\item{stage_index}{Integer. Which stage to vary. Takes priority over
stage_name if both are supplied.}

\item{stage_name}{Character. Column name of the focal stage in T_mat.}

\item{Vc_range}{Numeric vector of (Vc/c_bar) values to test.
Default seq(0.5, 5, by = 0.5).}

\item{Vc_fixed}{Numeric. (Vc/c_bar) for all OTHER stages. Default 1.}

\item{Vk_over_k}{Sexual variance vector. Default rep(1, s).}

\item{a}{Hardy-Weinberg deviation. Default 0.}

\item{L}{Generation time. If NULL, computed internally.}

\item{Ne_target}{Ne conservation threshold. Default 5000.}

\item{population}{Character label.}
}
\value{
A list with:
\describe{
\item{data}{Data frame: Vc_over_c, NeN, Min_N, L, V, V_term1,
V_term2, V_term3}
\item{plot}{ggplot2 object showing Ne/N vs Vc/c_bar}
\item{call}{Matched call}
}
}
\description{
Sweeps (Vc/c_bar)_i across a range for a single focal stage. Mixed model
only. Under Poisson defaults (Vc/c_bar = 1 = Vk/k_bar), clonal fraction
d_i has no effect on Ne. Non-Poisson clonal variance (Vc/c_bar > 1) is
required for d_i to influence Ne.
}
\examples{
T_herb <- matrix(c(
  0.30, 0.05, 0.00,
  0.40, 0.65, 0.10,
  0.00, 0.20, 0.80
), nrow = 3, byrow = TRUE)

sens <- Ne_sensitivity_Vc(
  T_mat       = T_herb,
  F_vec       = c(0.0, 0.5, 3.0),
  D           = c(0.60, 0.25, 0.15),
  d           = c(0.0, 0.0, 0.7),
  stage_index = 3,
  Vc_range    = seq(0.5, 6, by = 0.5),
  population  = "mixed herb"
)
print(sens$data)
sens$plot

}
