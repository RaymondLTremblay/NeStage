% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Ne_sensitivity.R
\name{Ne_sensitivity_Vk}
\alias{Ne_sensitivity_Vk}
\title{Sensitivity of Ne/N to sexual reproductive variance in one stage}
\usage{
Ne_sensitivity_Vk(
  model_fn,
  T_mat,
  F_vec,
  D,
  d = NULL,
  stage_index = NULL,
  stage_name = NULL,
  Vk_range = seq(0.5, 5, by = 0.5),
  Vk_fixed = 1,
  Vc_over_c = NULL,
  a = 0,
  L = NULL,
  Ne_target = 5000,
  population = NULL
)
}
\arguments{
\item{model_fn}{The model function to use: Ne_sexual_Y2000 or
Ne_mixed_Y2000. Do NOT supply Ne_clonal_Y2000 -- sexual
reproductive variance does not enter that model.}

\item{T_mat}{Survival/transition matrix (s x s).}

\item{F_vec}{Fecundity vector (length s).}

\item{D}{Stage frequency distribution (length s, sums to 1).}

\item{d}{Per-stage clonal fraction vector. Required if
model_fn = Ne_mixed_Y2000; ignored otherwise.}

\item{stage_index}{Integer. Which stage to vary. Takes priority over
stage_name if both are supplied.}

\item{stage_name}{Character. Column name of the focal stage in T_mat.
Used only if stage_index is NULL.}

\item{Vk_range}{Numeric vector of (Vk/k_bar) values to test.
Default seq(0.5, 5, by = 0.5).}

\item{Vk_fixed}{Numeric. (Vk/k_bar) for all OTHER stages. Default 1.}

\item{Vc_over_c}{Clonal variance vector (for Ne_mixed_Y2000 only).
Default rep(1, s).}

\item{a}{Hardy-Weinberg deviation. Default 0.}

\item{L}{Generation time. If NULL, computed internally.}

\item{Ne_target}{Ne conservation threshold. Default 5000.}

\item{population}{Character label.}
}
\value{
A list with:
\describe{
\item{data}{Data frame: Vk_over_k, NeN, Min_N, L, V, V_term1, V_term2,
and V_term3 (for mixed model)}
\item{plot}{ggplot2 object showing Ne/N vs Vk/k_bar}
\item{call}{Matched call}
}
}
\description{
Sweeps (Vk/k_bar)_i across a range for a single focal stage, holding all
other inputs constant. Works with Ne_sexual_Y2000 and Ne_mixed_Y2000.
}
\examples{
T_herb <- matrix(c(
  0.30, 0.05, 0.00,
  0.40, 0.65, 0.10,
  0.00, 0.20, 0.80
), nrow = 3, byrow = TRUE)

sens <- Ne_sensitivity_Vk(
  model_fn    = Ne_sexual_Y2000,
  T_mat       = T_herb,
  F_vec       = c(0.0, 0.5, 3.0),
  D           = c(0.60, 0.25, 0.15),
  stage_index = 3,
  Vk_range    = seq(0.5, 6, by = 0.5),
  population  = "hypothetical herb"
)
print(sens$data)
sens$plot

}
